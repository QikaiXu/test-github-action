on: 
  push:
  watch: 
    types: [ started ]

jobs:
  test-secret:
    runs-on: ubuntu-latest
    steps:
      - name: test-secret
        env:
          USER: ${{ secrets.USER }}
        run: |
          echo ${USER}
          echo ${#USER}
          for u in ${USER}
          do
            echo ${#u}
          done


